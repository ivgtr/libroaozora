{
  "name": "@libroaozora/workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit && tsc --noEmit -p scripts/tsconfig.json",
    "lint:test": "tsc --noEmit -p tests/tsconfig.json",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "sync": "tsx scripts/sync-metadata.ts"
  },
  "dependencies": {
    "@libroaozora/core": "workspace:*",
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.0",
    "@cloudflare/workers-types": "^4.20250327.0",
    "@types/node": "^25.5.0",
    "@vitest/runner": "^4.1.2",
    "@vitest/snapshot": "^4.1.2",
    "tsx": "^4.19.0",
    "vitest": "^4.1.2",
    "wrangler": "^4.10.0"
  }
}
